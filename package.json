{
  "name": "nokijs",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "stub": "turbo run stub",
    "publish:npm": "turbo run build && pnpm publish -r --access public",
    "publish:jsr": "turbo run publish:jsr"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.1",
    "turbo": "^2.1.2",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "packageManager": "pnpm@9.7.1"
}
